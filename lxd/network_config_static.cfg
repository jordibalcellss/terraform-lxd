version: 1
config:
  - type: physical
    name: eth0
    subnets:
      - type: static
        address: ${address}
        gateway: ${gateway}
        dns_nameservers:
          - ${dns_1}
          - ${dns_2}
        dns_search:
          - ${domain}
